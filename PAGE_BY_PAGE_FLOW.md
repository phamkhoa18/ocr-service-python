# ✅ Flow mới: OCR từng trang → Gọi ChatGPT ngay

## 🎯 Flow hoạt động

```
┌─────────────────────────────────────────────┐
│  PDF với nhiều trang                        │
└──────────────────┬──────────────────────────┘
                   │
        ┌──────────┴──────────┐
        │                     │
        ▼                     ▼
┌──────────────┐    ┌──────────────┐
│  Trang 1     │    │  Trang 2     │
└──────┬───────┘    └──────┬───────┘
       │                   │
       ▼                   ▼
┌──────────────┐    ┌──────────────┐
│  PaddleOCR   │    │  PaddleOCR   │
│  trang 1     │    │  trang 2     │
└──────┬───────┘    └──────┬───────┘
       │                   │
       ▼                   ▼
┌──────────────┐    ┌──────────────┐
│  Raw Text 1  │    │  Raw Text 2  │
└──────┬───────┘    └──────┬───────┘
       │                   │
       ▼                   ▼
┌──────────────┐    ┌──────────────┐
│  ChatGPT     │    │  ChatGPT     │
│  (GPT-4o)    │    │  (GPT-4o)    │
│  Sửa trang 1 │    │  Sửa trang 2 │
└──────┬───────┘    └──────┬───────┘
       │                   │
       ▼                   ▼
┌──────────────┐    ┌──────────────┐
│  Corrected 1 │    │  Corrected 2 │
└──────┬───────┘    └──────┬───────┘
       │                   │
       └──────────┬────────┘
                  │
                  ▼
         ┌─────────────────┐
         │  Combine tất cả │
         │  các trang      │
         └─────────────────┘
```

## 📝 Code Flow

### Loop qua từng trang:

```python
for idx, img in enumerate(images):
    # 1. OCR trang hiện tại
    print(f"Đang OCR trang {idx + 1}/{len(images)}...")
    result = ocr_image(img)
    page_text = result['text']
    
    # 2. Gọi ChatGPT ngay để sửa chính tả trang này
    print(f"  → Gọi ChatGPT (GPT-4o-mini) để chỉnh sửa chính tả trang {idx + 1}...")
    page_text = correct_vietnamese_text(page_text)  # Gọi API ngay
    print(f"  ✅ Đã sửa chính tả trang {idx + 1} xong")
    
    # 3. Lưu text đã được sửa
    all_texts.append(f"--- Trang {idx + 1} ---\n{page_text}")
    
    # 4. Tiếp tục trang tiếp theo...

# 5. Kết hợp tất cả các trang đã được sửa
combined_text = "\n\n".join(all_texts)
```

## ✅ Ưu điểm

- ✅ **Gọi ChatGPT ngay** - Không đợi OCR hết
- ✅ **Từng trang** - Dễ kiểm tra từng trang
- ✅ **Không mất data** - Nếu có lỗi ở trang nào, vẫn có các trang khác
- ✅ **Real-time** - User có thể thấy progress từng trang

## 🎯 Ví dụ

PDF 3 trang:

1. **Trang 1**: OCR → ChatGPT sửa → Lưu
2. **Trang 2**: OCR → ChatGPT sửa → Lưu  
3. **Trang 3**: OCR → ChatGPT sửa → Lưu
4. **Kết hợp**: Tất cả 3 trang đã được sửa

✅ **Đúng như user yêu cầu!**

